# creationAPI
Dans ce projet on cherche à créer une API et on utilisera l'interface de Postman pour les différentes requêtes.
##### IMPORTATION MODULES 
Il faut d'abord installer le module sqlalchemy;
Ensuite installer requests;

Pour l'installation des modules vous pouvez utiliser la commande suivante: pip install <nom_du_module>;
